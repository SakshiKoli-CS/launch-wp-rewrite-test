/** @type {import('next').NextConfig} */
const nextConfig = {
  // This tells Next.js to compile your app as a "dumb" static HTML site
  // instead of a Node.js server application.
  output: 'export',
};

module.exports = nextConfig;
